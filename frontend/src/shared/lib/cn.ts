import { cn as heroCn } from '@heroui/react';

/**
 * Joins class names and resolves Tailwind conflicts (later classes win).
 * Re-exported from HeroUI (tailwind-merge) so the whole app uses one implementation.
 */
export const cn = heroCn;
