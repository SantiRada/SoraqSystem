/** Public API of the account feature: account menu + Perfil pages (profile, billing, session, preferences). */
export { AccountMenu } from './components/AccountMenu';
export { accountRoutes } from './routes';
